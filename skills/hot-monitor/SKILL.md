---
name: hot-monitor
description: "AI 热点监控与发现技能。监控关键词、发现热点、分析新闻真实性与热度。当用户提到以下任何场景时触发此技能：监控关键词、添加/删除/查看关键词、查热点、找新闻、AI 领域动态、最近有什么热点、触发扫描、查看热点报告、热点统计、科技新闻、大模型更新、产品发布、行业动态、热门话题、trending、hotspot、news monitoring。即使用户没有明确说'热点监控'，只要意图是获取某个领域/关键词的最新动态或新闻，都应使用此技能。"
---

# AI 热点监控技能 (Hot Monitor)

自包含的 AI 热点监控技能，无需后端服务、无需 API Key，装上即用。通过网页搜索和 RSS 聚合发现热点，利用 Agent 自身的 AI 能力做真假甄别、摘要生成和热度评分。

## 能力概览

| 能力 | 说明 |
|------|------|
| 关键词管理 | 添加、删除、查看、启停监控关键词 |
| 热点发现 | 多源搜索 + AI 分析，返回筛选后的高质量热点 |
| 热点分析 | 真假甄别、中文摘要、热度评分（1-10）、去重 |
| 结果持久化 | 本地 JSON 文件存储关键词和历史结果 |
| 通知推送 | 直接在对话中返回发现的热点 |

## 何时使用

- 用户说"帮我监控 XXX"、"添加关键词 XXX"、"我的关键词列表"
- 用户说"最近有什么热点"、"XXX 领域有什么新动态"、"查一下 XXX 的最新新闻"
- 用户说"扫描一下"、"刷新热点"、"帮我找找最新的 AI 新闻"
- 用户说"热点统计"、"今天有多少新热点"
- 用户提到任何领域的新闻监控或热点追踪需求

## 数据存储

在用户工作区根目录下创建 `.hot-monitor/` 目录：

```
.hot-monitor/
├── keywords.json          # 关键词列表
└── results/
    └── 2026-03-24.json    # 按日期存储的扫描结果
```

首次使用时自动创建目录结构。读取 `references/data-models.md` 了解完整的数据结构定义。

## 核心工作流

### 1. 关键词管理

当用户要求管理关键词时：

**添加关键词：**
1. 读取 `.hot-monitor/keywords.json`（不存在则初始化为 `{"keywords": []}`）
2. 检查是否重复（按 keyword 字段去重）
3. 追加新条目，写回文件
4. 向用户确认添加成功，并询问是否立即扫描

**删除/启停关键词：**
1. 读取 keywords.json
2. 按名称匹配，执行删除或翻转 enabled 状态
3. 写回文件，向用户确认

**查看关键词：**
1. 读取 keywords.json
2. 格式化为表格输出（名称、类型、状态、创建时间）

### 2. 热点发现（核心流程）

当用户要求扫描热点或查询某领域动态时，按以下步骤执行。读取 `references/search-strategies.md` 获取每个数据源的详细搜索策略。

**步骤 A — 确定搜索关键词：**
- 如果用户指定了具体关键词，使用该关键词
- 如果用户说"扫描全部"，读取 keywords.json 中 enabled 的关键词
- 为每个关键词生成 2-3 个语义变体（如 "GPT-5" → "GPT5"、"GPT-5 发布"、"GPT-5 release"）

**步骤 B — 多源搜索：**

优先使用 `firecrawl_search` 工具（最可靠）。如果不可用，回退到 `fetch_webpage` 抓取搜索引擎页面。

对每个关键词/变体，依次搜索：
1. **Web 搜索**（firecrawl_search，query=关键词，limit=10，sources=[web, news]）
2. **RSS 源**（fetch_webpage 抓取公开 RSS 的 XML，按关键词过滤）

可选增强（需要 API Key，首次使用时询问用户是否有）：
3. **Twitter**（如用户提供了 twitterapi.io 的 API Key）

合并所有来源的结果，按标题去重。

**步骤 C — AI 分析与过滤：**

对每条搜索结果，用你自身的 AI 能力做以下分析。读取 `references/analysis-prompts.md` 获取详细的分析模板。

对每条内容判断：
- **相关性** — direct（直接相关）/ indirect（间接提及）/ none（无关）
- **真实性** — 内容是否来自可靠来源、是否为真实新闻（非标题党/谣言/段子）
- **热度评分** — 1-10 分，综合来源权威性、互动量、时效性
- **中文摘要** — 一句话概括核心信息

过滤规则：
- 相关性为 none → 丢弃
- 热度 < 3 → 丢弃
- 发布时间超过 10 天 → 丢弃
- 同名不同领域的人/事物 → 判定无关
- 个人闲聊/段子无新闻价值 → 丢弃

**步骤 D — 输出结果：**

将通过分析的热点按热度降序排列，在对话中直接展示给用户：

```markdown
## 🔥 热点扫描结果 — "关键词" (2026-03-24)

发现 N 条相关热点：

### 1. [标题](原文链接)
- 📊 热度：8/10 | 🏷️ 来源：Twitter | 📅 2026-03-24
- 📝 AI 首次实现完全自主编程，无需人工干预即可完成复杂项目开发...
- ✅ 真实性：已验证 | 相关性：直接相关

### 2. [标题](原文链接)
...
```

**步骤 E — 持久化：**

将结果追加到 `.hot-monitor/results/YYYY-MM-DD.json`（不存在则创建）。

### 3. 查看历史结果

当用户要求查看历史或统计时：
1. 列出 `.hot-monitor/results/` 目录下的文件
2. 读取指定日期的 JSON 文件
3. 格式化输出（支持按热度、来源、时间筛选）

### 4. Twitter API Key（可选增强）

首次涉及 Twitter 数据时，询问用户：

> "Twitter 数据源需要 twitterapi.io 的 API Key。你有 API Key 吗？如果有，我可以将它保存到 `.hot-monitor/config.json` 中以便后续使用。如果没有，我会跳过 Twitter 源，仅使用免费的网页搜索和 RSS。"

如果用户提供了 Key，保存到 `.hot-monitor/config.json`：
```json
{
  "twitter_api_key": "用户提供的key"
}
```

Twitter 搜索策略见 `references/search-strategies.md` 的 Twitter 章节。

## 工具优先级

按可靠性和速度排序，优先使用排在前面的工具：

1. **firecrawl_search** — Web 搜索，最可靠，支持 web/news/images 多源
2. **firecrawl_scrape** — 单页抓取，用于深入读取搜索结果的详情页
3. **fetch_webpage** — 备选网页抓取，当 Firecrawl 不可用时使用
4. **firecrawl_agent** — 复杂研究任务，当上述工具结果不足时使用（异步，需轮询）

如果所有网络工具都不可用，告知用户当前环境缺少网络访问能力，无法执行热点扫描。

## 注意事项

- 搜索间隔：对同一来源不要在短时间内频繁请求，每次扫描控制在合理范围内
- 结果数量：每个关键词最多返回 10-15 条原始结果，AI 过滤后通常保留 3-8 条
- 语言偏好：优先返回中文内容，英文内容生成中文摘要
- 时效性：默认只关注最近 7 天内的内容
- 隐私：不要在对话中展示用户的 API Key 明文

## 参考文档

当需要更多细节时，读取以下参考文件：

| 文件 | 内容 | 何时读取 |
|------|------|---------|
| `references/search-strategies.md` | 各数据源的详细搜索策略、URL 模板、质量门槛 | 执行热点搜索时 |
| `references/analysis-prompts.md` | AI 分析的结构化 Prompt 模板、评分标准 | 分析搜索结果时 |
| `references/data-models.md` | keywords.json 和 results JSON 的完整 Schema | 读写数据文件时 |
